---
aptly_repo_user: repo
aptly_repo_group: repo
aptly_repo_user_home: "/home/{{ aptly_repo_user }}"
aptly_gpg_generate_key: True
aptly_gpg_key_name: repo
aptly_repos: []
aptly_webroot_path: "{{ aptly_repo_user_home}}/.aptly/public"
